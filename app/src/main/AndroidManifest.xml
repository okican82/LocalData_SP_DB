<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.seng405.localdata_sp_db">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LocalData_SP_DB">
        <activity
            android:name=".Activities.DisplayPersonActivity"
            android:exported="false" />

        <activity
            android:name=".Activities.AddPersonActivity"
            android:exported="true"></activity>
        <activity
            android:name=".Activities.PhoneBookListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SharedPreferencesActivity"
            android:exported="true"></activity>
        <activity
            android:name=".ListViewDataSourceIsResourcesActivity"
            android:exported="true"></activity>
        <activity
            android:name=".ListViewActivity"
            android:exported="true" />
    </application>

</manifest>